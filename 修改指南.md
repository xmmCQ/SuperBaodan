# SuperBaodan 修改指南（建议稿）

> 基于 2026-09-04 对当前代码、运行界面及测试套件的检查结果编写。

## 一、修改目标

本轮修改以“降低后续维护成本”为主，不追求继续堆功能。

核心目标：

1. 合并首页与展开助手中重复的 Pi、会话和 SSE 逻辑。
2. 消除 `assistant.css` 与 `assistant-theme.css` 的大面积覆盖关系。
3. 拆分巨石文件，明确前后端模块职责。
4. 减少绑定源码写法的测试，增加真正验证用户行为的测试。
5. 优化设置页、首页首屏和小按钮的易用性。
6. 整理源码备份及日志。

## 二、基本原则

- 保留原生 Node.js + ES Module 架构。
- 不引入 React、Vue、Express 等框架。
- 不新增运行时依赖，继续保持无需本地 `node_modules`。
- 不改变现有 REST、SSE、Pi JSONL RPC 协议。
- 不做移动端适配，继续保持桌面端设计。
- 每个阶段单独提交、单独验证，禁止一次性大改。
- 重构阶段原则上不改变界面文字和业务行为。

## 三、实施优先级

| 优先级 | 工作项 | 收益 | 风险 | 工作量 |
|---|---|---|---|---|
| P0 | 建立 Git 基线并整理源码备份 | 防止重构失控 | 低 | 小 |
| P0 | 抽取共享助手客户端 | 消除双份状态逻辑 | 中 | 中 |
| P0 | 合并助手 CSS | 降低界面维护成本 | 中 | 中 |
| P1 | 拆分 `assistant.js`、`app.js` | 降低巨石文件复杂度 | 中 | 大 |
| P1 | 拆分 `server.mjs` 路由 | 明确后端边界 | 中 | 中 |
| P1 | 整理测试体系 | 降低脆弱测试数量 | 中 | 中 |
| P2 | 优化设置页和首页首屏 | 提高日常使用效率 | 低 | 中 |
| P2 | 增加恢复、自检和日志管理 | 提高故障恢复能力 | 低 | 中 |

---

## 四、第一阶段：建立可回退基线

### 4.1 建立 Git 仓库

当前项目根目录没有 `.git`，但 `data/backups` 中存在大量源码快照。修改前应先建立正式版本基线。

建议操作：

1. 初始化 Git。
2. 提交当前完整可运行版本，标签建议为 `baseline-before-refactor`。
3. 将运行数据、日志和会话排除在 Git 外。
4. 暂时不要删除旧备份；待新架构稳定后再归档。

建议补充 `.gitignore`：

```gitignore
data/sessions/
data/server.log
data/server-error.log
data/backups/
*.tmp
```

### 4.2 整理备份

当前 `data/backups` 约有：

- 315 个文件
- 87 个备份目录
- 4.38MB 数据

建议区分：

```text
data/backups/          仅保留程序自动生成的数据备份
D:\SuperBaodanArchive  人工历史源码快照，迁移至项目外
Git                    正式保存源码历史
```

### 验收标准

- 当前版本可通过 Git 标签完整恢复。
- 删除项目内源码快照后，不影响程序启动。
- 待办、工作区、VSkill 和会话数据仍正常读取。

---

## 五、第二阶段：抽取共享助手客户端

### 5.1 当前问题

以下两个文件分别维护了一套相似逻辑：

- `public/app.js`：首页内嵌助手
- `public/assistant.js`：展开助手

重复职责包括：

- Pi 启动和状态恢复
- SSE 建连、断线和重连
- 消息同步
- 会话加载、切换、重命名
- 新建会话、发送、停止
- 工作区参数拼接
- 8 个共同 API

这类重复不一定逐行相同，但属于“双份状态机”，后续容易出现两个界面行为不一致。

### 5.2 建议目录

```text
public/core/
├─ api-client.js
├─ agent-client.js
├─ event-stream.js
└─ session-service.js
```

### 5.3 模块职责

#### `api-client.js`

负责：

- JSON 请求和响应解析
- 统一错误转换
- 工作区 ID 注入
- 超时及请求取消

建议接口：

```js
api(path, options)
workspaceUrl(path, workspaceId)
workspacePayload(payload, workspaceId)
```

#### `event-stream.js`

负责：

- 创建和关闭 `EventSource`
- 连接状态维护
- 自动重连提示
- JSON 事件解析
- 防止页面重复建立连接

建议接口：

```js
createAgentEventStream({ onEvent, onStatus })
```

#### `session-service.js`

负责：

- 获取会话列表
- 新建、激活、重命名、删除会话
- 同步当前会话消息

只返回数据，不直接操作 DOM。

#### `agent-client.js`

负责：

- bootstrap
- send、stop、compact 等 Agent 命令
- 维护与界面无关的运行状态
- 将原始事件转换为统一状态变化

### 5.4 迁移顺序

1. 先抽取 `api-client.js`，不改变现有界面逻辑。
2. 再抽取 `session-service.js`。
3. 再抽取 `event-stream.js`。
4. 最后统一首页和展开助手的 Agent 事件处理。
5. 每抽取一个模块，都保持原文件对外行为不变。

### 5.5 不应抽取的内容

以下内容仍由各自页面负责：

- 首页的小尺寸消息渲染
- 展开助手的思考过程、工具调用和文件区
- 各页面自己的 DOM 引用
- 首页待办和日历逻辑

### 验收标准

- 首页和展开助手共用同一个 API、会话和 SSE 模块。
- 同一 Pi 事件在两个页面中的状态结论一致。
- 刷新、休眠唤醒、SSE 断线恢复行为不变。
- 首页仍可原地对话，展开助手仍可展示完整工具过程。

---

## 六、第三阶段：合并助手 CSS

### 6.1 当前问题

`assistant-theme.css` 有大量规则实际是在重新覆盖 `assistant.css`。按选择器归一化后，主题文件约 73% 的选择器会触及基础样式。

这会导致：

- 修改一个组件时需要同时查两个文件。
- 样式优先级依赖 `body.assistant-page` 前缀。
- 容易出现“基础样式改了但被主题覆盖”的问题。

### 6.2 建议结构

```text
public/
├─ assistant.css            布局与组件结构
├─ assistant-tokens.css     颜色、圆角、阴影、间距变量
├─ icon-actions.css
├─ markdown.css
├─ ui-dialog.css
└─ workspace-switcher.css
```

`assistant-theme.css` 合并完成后删除。

### 6.3 修改方法

1. 将主题中的颜色值归纳为 CSS 变量。
2. 将圆角、阴影、边框归纳为设计变量。
3. 同一选择器只保留一处定义。
4. 去掉仅用于提高优先级的 `body.assistant-page` 前缀。
5. 不在本阶段修改布局尺寸和视觉风格。

建议变量：

```css
:root {
  --color-accent: #3b82f6;
  --color-accent-strong: #1d4ed8;
  --color-surface: #ffffff;
  --color-surface-soft: #f3f8ff;
  --color-border: #d9e6f5;
  --radius-control: 999px;
  --radius-card: 14px;
  --shadow-soft: 0 6px 18px rgba(43, 87, 142, .08);
}
```

### 验收标准

- 删除 `assistant-theme.css` 后界面无明显视觉变化。
- 不再存在同一组件跨两个主样式文件覆盖的情况。
- 设置页、文件区、聊天区和会话侧栏均正常显示。

---

## 七、第四阶段：拆分前端巨石文件

### 7.1 拆分 `public/assistant.js`

当前约 1869 行，建议拆为：

```text
public/assistant/
├─ state.js
├─ chat-view.js
├─ sessions-view.js
├─ models-controller.js
├─ auth-controller.js
├─ skills-controller.js
├─ workspace-controller.js
├─ settings-dialog.js
└─ main.js
```

职责建议：

- `state.js`：共享状态及状态变更入口。
- `chat-view.js`：消息、思考、工具调用、队列渲染。
- `sessions-view.js`：历史会话搜索及操作。
- `models-controller.js`：模型选择、思考等级及配置。
- `auth-controller.js`：OAuth 和 API Key 操作。
- `skills-controller.js`：Skill 列表、搜索、安装和编辑。
- `workspace-controller.js`：文件树、搜索、上传和预览。
- `settings-dialog.js`：设置页签及弹窗协调。
- `main.js`：初始化和模块装配。

优先拆分体积较大、边界较清晰的模型、Skill、工作区模块，最后再拆聊天核心。

### 7.2 拆分 `public/app.js`

当前约 1426 行，建议拆为：

```text
public/home/
├─ dashboard.js
├─ calendar.js
├─ tasks.js
├─ home-chat.js
├─ vskills.js
└─ main.js
```

### 7.3 控制原则

- 模块之间通过函数参数或自定义事件通信。
- 不允许所有模块直接读写一个巨大可变对象。
- 渲染函数不直接发请求。
- 请求模块不直接修改 DOM。
- 避免循环 import。

### 验收标准

- 单个业务文件原则上不超过 500 行。
- `main.js` 只负责启动和装配。
- 模块可单独理解，不需要阅读全文才能修改一个功能。

---

## 八、第五阶段：拆分服务端路由

### 8.1 当前问题

`server.mjs` 同时承担：

- 服务初始化
- 56 个 HTTP method 分支
- Agent、会话、认证、模型、Skill、工作区、待办路由
- SSE 广播
- 静态文件服务
- 工作区切换协调
- 待办文件持久化

服务规模对个人项目仍可运行，但已经超过单文件易维护范围。

### 8.2 建议目录

```text
server/
├─ app.mjs
├─ router.mjs
├─ response.mjs
├─ runtime-context.mjs
└─ routes/
   ├─ agent.mjs
   ├─ sessions.mjs
   ├─ auth.mjs
   ├─ models.mjs
   ├─ skills.mjs
   ├─ workspaces.mjs
   ├─ tasks.mjs
   └─ system.mjs
```

根目录 `server.mjs` 只负责：

1. 创建运行时上下文。
2. 注册路由。
3. 启动 HTTP 服务。
4. 处理优雅退出。

### 8.3 简易路由方式

不需要引入 Express，可实现轻量路由表：

```js
router.get("/api/health", healthHandler);
router.post("/api/agent/command", agentCommandHandler);
router.patch(/^\/api\/tasks\/([^/]+)$/, updateTaskHandler);
```

### 8.4 运行时上下文

将当前散落的全局变量收敛为一个上下文对象：

```js
const context = {
  activeWorkspace,
  piRuntime,
  piAdmin,
  skillManager,
  workspaceService,
  eventClients,
  queues,
};
```

路由通过显式参数接收上下文，避免隐式依赖模块全局变量。

### 验收标准

- 原有 API 路径、方法、状态码和响应结构不变。
- 工作区切换、维护锁及任务写入队列仍有效。
- `server.mjs` 控制在约 150 行以内。
- 路由文件不直接创建全局单例。

---

## 九、第六阶段：整理测试体系

### 9.1 应保留的测试

优先保留真正验证输入输出和异常行为的测试：

- `tasks.test.mjs`
- `task-writer.test.mjs`
- `workspace.test.mjs`
- `workspace-registry.test.mjs`
- `skill-manager.test.mjs`
- `pi-admin.test.mjs`
- `pi-rpc-lifecycle*.test.mjs`
- `vskill-manager.test.mjs`

### 9.2 应合并的测试

建议将以下测试中的产品约束合并为一个文件：

```text
test/product-constraints.test.mjs
```

可合并内容：

- 桌面端专用约束
- 已移除会话分支
- 已移除工作区 Git 状态
- 内部待办文件位置

### 9.3 应删除或改写的断言

删除：

- `?v=26`、`?v=45` 等缓存版本断言。
- 对完整 CSS 字符串和精确排列顺序的断言。
- 只验证某个函数名是否存在的断言。

改写：

- “源码中存在某段字符串”改为“页面加载后对应控件可见”。
- “代码调用某个 API”改为“操作后产生预期请求和界面状态”。
- “旧功能字符串不存在”改为集中式产品约束检查。

### 9.4 抽取测试工具

将两个 Pi RPC 生命周期测试中的假子进程抽出：

```text
test/helpers/controlled-pi-child.mjs
```

将临时目录、JSON 文件和清理逻辑抽出：

```text
test/helpers/temp-project.mjs
```

### 9.5 增加少量浏览器冒烟测试

建议覆盖五条主链路：

1. 首页完成加载并隐藏 loading 层。
2. 新增、编辑、完成、改期和删除待办。
3. 助手 bootstrap、SSE 事件和消息恢复。
4. 新建、切换、重命名和删除会话。
5. 设置页打开、工作区文件上传和预览。

浏览器测试必须使用临时数据目录和独立端口，不能操作正式待办及会话数据。

### 验收标准

- 测试数量可以减少，但关键业务行为覆盖不能下降。
- 修改 CSS 类名时不应引发大量无业务意义的测试失败。
- 所有测试仍应在数秒内完成。

---

## 十、第七阶段：操作和界面优化

### 10.1 设置页

当前设置页一次展示大量供应商卡片。建议：

- 增加供应商搜索。
- 默认优先展示“已登录、已设置、常用”。
- 未配置供应商放入“更多供应商”折叠区。
- OAuth 与 API Key 两类继续分组。
- 保留当前模型数量和登录状态信息。

### 10.2 小按钮热区

建议所有操作按钮的可点击区域至少为：

```text
32 × 32px，主要操作建议 36 × 36px
```

重点调整：

- 待办完成按钮
- 待办编辑、删除按钮
- 会话重命名、删除按钮

图标本身可保持较小，通过 padding 放大热区。

### 10.3 首页首屏

当前完整日历占用较高，遗留工作和长期工作内容进入第二屏。

建议二选一：

#### 方案 A：压缩日历

- 月份只展示实际需要的 5 或 6 行。
- 降低空日期格高度。
- 保持当前三栏布局。

#### 方案 B：调整优先级

- 第一行：当日详情、遗留工作、工作助手。
- 第二行：工作日历、长期工作。

推荐先采用方案 A，改动较小。

### 10.4 统一视觉语言

建议统一：

- 产品名称：统一使用“超级宝蛋”。
- 主头像和助手头像。
- 首页绿色与展开助手蓝色之间的主品牌色。
- 卡片圆角、边框和阴影等级。

不要求两个页面完全相同，但应让用户感知为同一产品。

### 10.5 VSkill 入口

首页助手顶部的“V”识别度不足，建议替换为：

- 魔法棒图标；或
- 闪电图标；或
- “快捷指令”文字按钮。

### 验收标准

- 常用供应商可在两步内找到。
- 所有高频操作按钮热区不小于 32px。
- 1440×900 桌面视口能够看到至少一部分遗留工作或长期工作内容。
- 首页和展开助手的品牌名称、主色及头像关系明确。

---

## 十一、第八阶段：补充恢复和自检能力

### 11.1 备份恢复

增加只读备份列表及恢复入口，覆盖：

- 工作待办
- VSkill 配置
- 工作区配置
- 模型配置
- 自定义 Skill

恢复前再次创建当前版本备份，并显示备份时间和内容类型。

### 11.2 故障自检

建议在设置页增加“系统状态”页签，展示：

- Node.js 版本
- Pi 版本
- Pi 是否已安装
- 当前工作区
- 数据目录和会话目录
- RPC 状态
- 最近一次异常
- 重新启动 Pi 按钮

### 11.3 日志调整

日志建议采用：

```text
2026-09-04T10:30:20.123+08:00 INFO  server started port=3211
2026-09-04T10:31:05.221+08:00 ERROR skills search timeout
```

并增加：

- 时间戳
- INFO/WARN/ERROR 等级
- 单文件大小上限
- 保留最近若干份日志
- 每次服务启动写入明显分隔线

### 验收标准

- 用户无需查文件即可判断 Pi、服务和工作区状态。
- 最近一次有效备份可从界面恢复。
- 历史错误不会与当前启动周期混在一起。

---

## 十二、建议提交顺序

建议按以下顺序形成独立提交：

```text
1. chore: establish git baseline and ignore runtime data
2. refactor: extract shared api client
3. refactor: extract shared session service
4. refactor: extract shared agent event stream
5. refactor: merge assistant theme styles
6. refactor: split assistant models skills and workspace modules
7. refactor: split home dashboard and chat modules
8. refactor: split server routes
9. test: consolidate source contract tests
10. test: add browser smoke tests
11. ui: simplify provider settings
12. ui: improve home hierarchy and action targets
13. feat: add backup recovery and system diagnostics
14. chore: add structured log rotation
```

## 十三、最终验收清单

### 功能

- [ ] 首页待办全部操作正常。
- [ ] 首页助手和展开助手均可发送、停止和恢复消息。
- [ ] 会话新建、切换、重命名和删除正常。
- [ ] 工作区切换、搜索、上传和预览正常。
- [ ] 模型、登录、思考等级和 Skill 管理正常。
- [ ] Pi 休眠后可自动唤醒并恢复会话。

### 界面

- [ ] 首页及助手视觉无明显回退。
- [ ] 设置页支持快速查找供应商。
- [ ] 高频按钮热区不小于 32px。
- [ ] 首页首屏信息优先级合理。

### 代码

- [ ] 首页与展开助手不再维护两套 API/SSE/会话底层逻辑。
- [ ] `assistant-theme.css` 已删除或仅保留极少数真正主题规则。
- [ ] 前端主要业务文件原则上不超过 500 行。
- [ ] `server.mjs` 只负责初始化和启动。
- [ ] 不新增不必要的第三方依赖。

### 测试

- [ ] 后端核心单元测试全部通过。
- [ ] UI 测试不再依赖缓存版本号。
- [ ] 主流程浏览器冒烟测试通过。
- [ ] 测试使用临时数据，不修改正式工作资料。

## 十四、推荐本轮范围

为控制风险，第一轮建议只做以下四项：

1. 建立 Git 基线并整理源码备份。
2. 抽取共享 API、会话及 SSE 模块。
3. 合并 `assistant.css` 与 `assistant-theme.css`。
4. 清理缓存版本号测试及重复测试桩。

完成这四项后先稳定使用一段时间，再决定是否继续拆分完整前后端。这样收益最大，且不会因一次性重构过多影响日常使用。
